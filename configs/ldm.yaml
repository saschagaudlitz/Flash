model_params:
  name: LDM
  n_dim: 6
  n_latent_dim: 6
  n_timesteps: 5
  n_unet_dim: 8
  n_resnet_block_groups: 1

experiment_params:
  learning_rate: 0.001
  manual_seed: 1337

data_params:
  batch_size: 32
  val_split_size: 0.2

trainer_params:
  max_epochs: 10
